
&Вместо("ОбработатьЗапрос")
Функция Мок_ОбработатьЗапрос(Знач Обработчик, Знач ВнешнийЗапрос) Экспорт

	ПараметрыМетода = ОМ_Тесты.Мокито().МассивПараметров(Обработчик, ВнешнийЗапрос);
	
	ПрерватьВыполнение = Ложь;
	Результат = ОМ_Тесты.Мокито().АнализВызова(ОбработкаДанных, "ОбработатьЗапрос", ПараметрыМетода, ПрерватьВыполнение);
	
	Если НЕ ПрерватьВыполнение Тогда
		Возврат ПродолжитьВызов(Обработчик, ВнешнийЗапрос);
	Иначе
		Возврат Результат;
	КонецЕсли;

КонецФункции
