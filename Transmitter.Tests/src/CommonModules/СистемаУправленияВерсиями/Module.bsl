
&Вместо("ПолучитьКраткийИдентификатор")
Функция Мок_ПолучитьКраткийИдентификатор(Знач Идентификатор) Экспорт

	ПараметрыМетода = ОМ_Тесты.Мокито().МассивПараметров(Идентификатор);
	
	ПрерватьВыполнение = Ложь;
	Результат = ОМ_Тесты.Мокито().АнализВызова(СистемаУправленияВерсиями, "ПолучитьКраткийИдентификатор", ПараметрыМетода, ПрерватьВыполнение);
	
	Если НЕ ПрерватьВыполнение Тогда
		Возврат ПродолжитьВызов(Идентификатор);
	Иначе
		Возврат Результат;
	КонецЕсли;

КонецФункции
