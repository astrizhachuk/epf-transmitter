
&Вместо("ОтправитьФайлыВФоне")
Функция Мок_ОтправитьФайлыВФоне(Знач Файлы) Экспорт

	ПараметрыМетода = ОМ_Тесты.Мокито().МассивПараметров(Файлы);
	
	ПрерватьВыполнение = Ложь;
	Результат = ОМ_Тесты.Мокито().АнализВызова(Получатели, "ОтправитьФайлыВФоне", ПараметрыМетода, ПрерватьВыполнение);
	
	Если НЕ ПрерватьВыполнение Тогда
		Возврат ПродолжитьВызов(Файлы);
	Иначе
		Возврат Результат;
	КонецЕсли;

КонецФункции
