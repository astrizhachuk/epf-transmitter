
&Вместо("РаспределитьФайлыПоМаршрутам")
Функция Мок_РаспределитьФайлыПоМаршрутам(Знач Схемы, Знач Файлы) Экспорт

	ПараметрыМетода = ОМ_Тесты.Мокито().МассивПараметров(Схемы, Файлы);
	
	ПрерватьВыполнение = Ложь;
	Результат = ОМ_Тесты.Мокито().АнализВызова(Маршрутизация, "РаспределитьФайлыПоМаршрутам", ПараметрыМетода, ПрерватьВыполнение);
	
	Если НЕ ПрерватьВыполнение Тогда
		Возврат ПродолжитьВызов(Схемы, Файлы);
	Иначе
		Возврат Результат;
	КонецЕсли;

КонецФункции
