
&Вместо("ЗаполнитьСхемыМаршрутов")
Процедура Мок_ЗаполнитьСхемыМаршрутов(Знач Данные, Знач ИмяФайла) Экспорт

	ПараметрыМетода = ОМ_Тесты.Мокито().МассивПараметров(Данные, ИмяФайла);
	
	ПрерватьВыполнение = Ложь;
	ОМ_Тесты.Мокито().АнализВызова(ЭтотОбъект, "ЗаполнитьСхемыМаршрутов", ПараметрыМетода, ПрерватьВыполнение);
	
	Если НЕ ПрерватьВыполнение Тогда
		ПродолжитьВызов(Данные, ИмяФайла);
	КонецЕсли;

КонецПроцедуры
